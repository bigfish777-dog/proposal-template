# Proposal template system

These partials are the canonical source of truth for locked proposal sections.

- `partials/about-us.html`
- `partials/results.html`

Rule: generate only proposal-specific sections; insert these exact partials unchanged into every new proposal.

Current canonical source: `chris-miller/index.html`.
